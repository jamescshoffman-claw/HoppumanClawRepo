// Intentionally empty. This isolates the build from the parent site's
// postcss.config.js / tailwind.config.js, which PostCSS would otherwise pick
// up by searching up the directory tree. This app ships plain hand-written CSS
// (src/index.css) and uses no Tailwind.
export default {}
